/**
 * This is an experimental package, which *should* be easily put into an external project as-is (configuration apart)
 * and work just fine as if it were used internally.
 * <p>
 * A package containing the
 */
package fr.anisekai.library.remote;
